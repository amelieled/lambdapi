(rule
 (targets version.ml)
 (action
  (with-stdout-to version.ml
    (run ocaml unix.cma %{dep:gen_version.ml})))
 (mode fallback))

(executable
 (name lambdapi)
 (public_name lambdapi)
 (modules :standard)
 (libraries cmdliner lambdapi.lsp competitor_format))
